// src/components/Header.js
import React from 'react';
import './header.css'; // Make sure the import matches the filename casing

const Header = () => {
  return (
    <header className="header">
      <div className="header-content">
  
        {/* You can add more header elements here, like nav or buttons */}
      </div>
    </header>
  );
};

export default Header;
